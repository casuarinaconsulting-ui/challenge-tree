# Set this to your Railway backend URL in Vercel's environment variable settings
# Leave blank for local dev (the Vite proxy handles /api → localhost:4000)
VITE_API_URL=https://your-backend.railway.app
